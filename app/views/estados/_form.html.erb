<%= form_for(@estado) do |f| %>
  <% if @estado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@estado.errors.count, "error") %> prohibited this estado from being saved:</h2>

      <ul>
      <% @estado.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <% f.label :nome %><br />
    <%= f.select :nome, @estados_recrut.collect {|p| [ p.nome, p.nome ] } + ['Outro'] , :prompt => 'Escolha um'%>

    
    <%= f.text_field :nome,:placeholder =>"Nome estado" %>
  </div>

  <div class="field">
    <% f.label :data %>
    <%= f.hidden_field :data,value: Time.now %>
  </div>
  <div class="field">
    <% f.label :descricao %><br />
    <%= f.text_area :descricao ,:size=>"400*39",:class=>"allowEnter",:placeholder =>"Descrição"%>
  </div>
  <div class="field">
    <% f.label :responsavel %><br />
    <%= f.text_field :responsavel ,:placeholder =>"Responsavel"%>
  </div>

   <div class="field">
    <% f.label :candidato_id %><br />
    <%= f.text_field :candidato_id ,value: candidato %>
  </div>

  <button class="btn btn-primary"  href="#"><i class="icon-plus"></i> Adicionar Estado</button>

  
<% end %>
